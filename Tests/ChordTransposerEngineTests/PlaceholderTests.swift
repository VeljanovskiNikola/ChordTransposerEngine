import XCTest
@testable import ChordTransposerEngine

final class PlaceholderTests: XCTestCase {
    func testPackageLoads() {
        // Verify the package compiles and links correctly.
        XCTAssertTrue(true)
    }
}
